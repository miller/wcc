Chassis.PageView.define( 'send', {
    
    // 设置PageView所在的DOM
    el: '#page-send',

    // 配置事件
    events: {
        
    },

    init: function( opts ) {
        
    },

    // 在APP路由到当前页面之前会调用该方法
    onBeforePageIn: function() {

    }
} );